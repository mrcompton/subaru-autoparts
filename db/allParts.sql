-- select
-- parts.name,
-- parts.price,
-- parts.description,
-- parts.picture,
-- parts.part_num,
-- cars.year,
-- cars.model,
-- cars.trim
-- from cars
-- join subaru_key on cars.id = subaru_key.car_id
-- join parts on subaru_key.part_id = parts.id;

select * from parts